# cca-empty-repo3

A simple test repository with basic math functions and tests.

## Installation

```bash
npm install
```

## Running Tests

```bash
npm test
```

## Functions

- `add(a, b)` - Adds two numbers together
- `multiply(a, b)` - Multiplies two numbers together